allocer: main()

deallocation of invalid pointer 0xPTR
  called on:

deallocation of invalid pointer 0xPTR
  called on:

allocation failed of NUM bytes
  called on:

no leaks found
exit code: 7 (0xPTR)
