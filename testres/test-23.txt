allocer: main()

deallocation of invalid pointer 0xPTR
  called on:

double free of 0xPTR (size 16)
  called on:
  allocated on:
  freed on:

allocation failed of NUM bytes
  called on:

no leaks found
exit code: 7 (0xPTR)
